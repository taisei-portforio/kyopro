#include<bits/stdc++.h>
#include <iostream>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)

int main(){
    int n
    cin >> n 
    vector<int> s(n);
    rep(i, n) {
        cin >> s[i];
    }

    int q;
    cin >> q;
    vector<int> t(q);
    rep(i, q) {
        cin >> t[q];
    }

    


}
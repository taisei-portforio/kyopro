#include<bits/stdc++.h>
#include <iostream>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)
#define repd(i, n) for(int i = 0; i <= (n); ++i)
using ll = long long;
using Graph = vector<vector<int>>

int getDepth(u) {
    d = 0;
    while(T[u].parent != NIL) {
        u = T[u].parent;
        d++;
    }
    return d;
}  //木の深

int setDepth(u, p) { //再帰的な木の深さ
    D[u]
}

int main(){
    
}
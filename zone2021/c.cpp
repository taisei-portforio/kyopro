#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)
using ll = long long;

int main() {
    int n;
    cin >> n;
    vector<int> a[n];
    vector<int> b[n];
    vector<int> c[n];
    vector<int> d[n];
    vector<int> e[n];

    rep(i, n) {
        cin >> a[i] >> b[i] >> c[i] >> d[i] >> e[i];
    }

    int point = 0;
}
#include<bits/stdc++.h>
#include <iostream>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)
#define repd(i, n) for(int i = 1; i < (n); ++i)
using ll = long long;
using Graph = vector<vector<int>>;

int main(){
    int h, w;
    cin >> h >> w;
    vector<int> i(h);
    vector<int> j(w);
    vector<string> s;
    for(int i = 0; i < h; i++) {
        for(int j = 0; j < w; j++) {
            cin >> s[i][j];
        }
    }//入力

    for(int i = 0; i < h; i++) {
        for(int j = 0; j < w; j++) {
            if(s[2] == 'u') {
                
            }
        }
    }

}